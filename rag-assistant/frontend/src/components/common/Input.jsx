import React from 'react'

const Input = (props) => {
  return (
    <input style={{ padding: '0.5rem', flex: 1, border: '1px solid #ccc', borderRadius: '4px' }} {...props} />
  )
}

export default Input
